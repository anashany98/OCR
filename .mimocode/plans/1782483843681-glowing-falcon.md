# Plan: Complete F9 — Replace remaining `window.confirm` calls

## Status

28/29 AGENTS.md tasks are DONE. The only incomplete item is **F9 (Accessibility)**:
the `useConfirm` hook and `ConfirmDialogHost` exist, but 3 admin pages still use
`window.confirm`.

## What needs to change

Replace 4 `window.confirm(...)` calls with `useConfirm()` in:

1. **`src/pages/admin/AdminIntegrationsPage.tsx:147`** — API key rotation confirm
2. **`src/pages/admin/AdminOperationalPage.tsx:166`** — Pause queues confirm
3. **`src/pages/admin/AdminOperationalPage.tsx:176`** — Resume queues confirm
4. **`src/pages/admin/AdminQualityPage.tsx:311`** — Re-process confirm

## Pattern

Each file already has access to React hooks. The change per file is:

```tsx
// Add import
import { useConfirm } from "@/hooks/useConfirm"

// Inside the component:
const confirm = useConfirm()

// Replace:
//   if (window.confirm("¿Pausar?")) pauseQueues.mutate()
// With:
//   const ok = await confirm({ title: "¿Pausar?", description: "...", confirmLabel: "Pausar", tone: "danger" })
//   if (ok) pauseQueues.mutate()
```

The onClick handlers must become `async` to `await` the confirm promise.

## Verification

1. `npm run lint` — no new warnings
2. `npm run build` — builds without errors
3. Manual: each admin action button shows a themed confirmation dialog, not a browser-native alert
