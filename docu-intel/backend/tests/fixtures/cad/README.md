Fixtures CAD sintéticos y anónimos para CI. `fixture_simple.dxf` cubre
geometría cerrada, círculo, texto, capas y unidades mm. Las DIMENSION e
INSERT con atributos se generan en `test_cad_structured_implementation.py`
porque ezdxf necesita crear sus tablas de definición de forma coherente.
